
/**
 * Checks if a string is null or blank.
 * @param {*} str 
 * @returns boolean
 */
export const isBlankOrEmpty = (str) => {
    return str === null || str === undefined || String(str).trim() === "";
}

export const isWhitespace = () => {
    // TODO implement
}

export const isLetter = () => {
    // TODO implement
}

export const isDigit = () => {
    // TODO implement
}